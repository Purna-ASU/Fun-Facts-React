
function MainContent() {
    return(
      <main>
        <h1 className="heading">Fun Facts about React</h1>
        <ul className="list">
          <li>Was developed by Facebook's engineering team and was first released in 2013.</li>
          <li>One of the key features of React is its Virtual DOM (Document Object Model), which is a lightweight representation of the actual DOM.</li>
          <li>React encourages a component-based architecture, where user interfaces are broken down into reusable, self-contained components. </li>
          <li>React uses JSX (JavaScript XML) syntax, which allows developers to write HTML-like code within JavaScript.</li>
          <li>JSX makes the creation of UI components more intuitive and readable.</li>
        </ul>
      </main>
    )
  };

  export default MainContent;